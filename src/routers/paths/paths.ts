import { PathsStructure } from "./types";

const paths: PathsStructure = {
  homePage: "/home",
  places: "/places",
  newPlace: "/new-place",
  detailPlace: "/places/:placeId",
};

export default paths;
